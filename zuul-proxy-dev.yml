eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

server:
  port: 8080

security:
  oauth2:
    client:
      client-id: foo
      client-secret: bar
  resource:
    token-info-uri: http://auth-server:8763/oauth/check_token
    jwt:
      key-value: com.ikth.apps

  
zuul:
  routes:
    reservation-service: /api/reserve/**
    authorization-service: /api/auth/**
    web-service: /web/**